Configure a backend of type "OpenSearch" under Search Engine > Configuration
> Backends: host, authentication (API key or HTTP basic auth) and, if
needed, disable gzip compression of the payloads exchanged with the
cluster (enabled by default).
